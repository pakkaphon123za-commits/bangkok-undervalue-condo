# Copy to .env and fill in. .env is gitignored (never committed).
# opencode Go API key for LLM narratives (optional — stats-only fallback works without it).
OPENCODE_GO_API_KEY=
OPENCODE_GO_BASE_URL=
